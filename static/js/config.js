// ============================================================
// CONFIG — tunable values in one named place. Imports nothing from the
// app, so every layer can read it.
// ============================================================

// The comparison panels' right-hand default when side-by-side mode opens
// (the left one shows the current projection): Gall-Peters, the equal-area
// counterpoint to Mercator.
export const DEFAULT_COMPARISON_PROJECTION = "gallPeters";

// Animation durations in ms. The perf harness reads the polar-route legs
// from this block to size its recording window, so keep one key per line.
export const TIMING = {
  projectionMorph: 1400, // regular blend between two projections
  polarFold: 800,        // polar route: flat map → globe
  polarSpin: 700,        // polar route: globe turns to/from the pole
  polarUnfold: 800,      // polar route: globe → polar view
  recenterRotation: 900, // a view preset's rotation
  recenterFlip: 900,     // the upside-down view's coin flip
  cameraReset: 500,      // camera back to the default framing
  zoomButton: 200,       // one ± button press
  wheelZoom: 150,        // easing of one mouse-wheel notch
  countryFocus: 600,     // pan/zoom onto a selected country
};

// --- camera ---
// Camera zoom range, as scale factors of the default framing
export const MAIN_ZOOM_SCALE_EXTENT = [1, 40];

export const LIGHT_ZOOM_MAX_SCALE   = 3; // gentle zoom-in cap when centering on a clicked/searched country

export const ZOOM_STEP  = 1.3; // multiplicative factor per click, same feel as one mouse-wheel notch

// How close to the default transform counts as the camera being at rest
// (isCameraAtIdentity), so float noise doesn't trigger a reset animation
export const CAMERA_IDENTITY_EPSILON = 0.001;

export const GLOBE_DRAG_SENSITIVITY = 0.35; // degrees rotated per pixel dragged

// --- geometry ---
// Douglas-Peucker keeps the points that shape a coastline (capes, gulfs,
// peninsulas) and drops the ones lying on a straight run. At 0.3° it
// costs the same per frame as the plain spacing rule it replaced (drop
// any point within 1° of the last kept one), which flattened Italy,
// Greece and Norway into crude polygons. 0.2° looks slightly smoother but
// tripled the dropped frames in the headless perf run.
export const LIGHT_TOLERANCE_DEG = 0.3;

// Rings whose points all sit within this of their first point are islands
// too small to matter mid-morph (see thinPolygon for why they're skipped).
export const LIGHT_SPECK_DEG = 1;

// --- tissot ---
export const TISSOT_STEP   = 30; // degrees between grid points

export const TISSOT_RADIUS = 4;  // degrees — the geographic circle radius

// --- reference-lines ---
// A meridian every 15° — one per hour of Earth's rotation, the usual atlas spacing
export const MERIDIAN_STEP    = 15;

// --- compare-card ---
// How many countries the compare card takes at once
export const COMPARE_MAX = 5;

// --- theme ---
// localStorage key of the chosen theme
export const THEME_STORAGE_KEY = "mapProjektorTheme";

// --- data ---
// Served by app/main.py; the GeoJSON is generated by scripts/fetch_geodata.py
export const WORLD_GEOJSON_URL = "/data/world.geojson";
export const TERRAIN_GEOJSON_URL = "/data/terrain.geojson";
export const I18N_BASE_URL = "/static/i18n/"; // + <lang>.json
